H = {}

H.Mode = Model.Enum.Mode
H.Trackable = Model.Enum.Trackable
H.Metric = Model.Enum.Metric

H.Enum = {}

H.Enum.Flags = {
    IGNORE = "ignore",
}

H.Enum.Offsets = {
    ABILITY = 512,
    PET  = 512,
}

H.Enum.Text = {
    BLANK = "",
}