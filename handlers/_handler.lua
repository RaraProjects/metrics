H = {}

H.Mode = DB.Enum.Mode
H.Trackable = DB.Enum.Trackable
H.Metric = DB.Enum.Metric

H.Enum = {}

H.Enum.Flags = {
    IGNORE = "ignore",
}

H.Enum.Offsets = {
    ABILITY = 512,
    PET  = 512,
}

H.Enum.Text = {
    BLANK = "",
}